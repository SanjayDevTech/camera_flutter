name: camera_flutter
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
    sdk: '>=2.12.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter
    camera: ^0.8.1+7
    path_provider: ^2.0.2
    path: ^1.8.0

flutter:
    uses-material-design: true
    assets:
        - assets/
